<UserControl x:Class="MediaLibraryEditor.WPF.Views.TvShow.TvSeriesDetailsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="Auto" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="Auto"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock
            Grid.Row="0"
            Grid.Column="0"
            Text="Description:" />
        
        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            Text="{Binding Description}" />
        
        
        <Button
            Grid.Row="1"
            Grid.Column="1"
            HorizontalAlignment="Right"
            Content="Save"
            Command="{Binding SaveCommand}"/>
        
    </Grid>
</UserControl>
